{
  "name": "farkle",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json && npm run typecheck -w @farkle/web",
    "build": "npm run build -w @farkle/engine && npm run build -w @farkle/bots && npm run build -w @farkle/cli && npm run build -w @farkle/web",
    "play": "npm run build --silent && node apps/cli/dist/main.js",
    "dev:web": "npm run dev -w @farkle/web",
    "dice:roster": "npm run build --silent && node scripts/dice-balance/roster-report.mjs",
    "dice:sweep": "npm run build --silent && node scripts/dice-balance/sweep-candidates.mjs",
    "dice:audit": "npm run build --silent && node scripts/dice-balance/wildcard-audit.mjs",
    "dice:lab": "npm run build --silent && node scripts/dice-balance/loadout-lab.mjs",
    "dice:tiers": "npm run build --silent && node scripts/dice-balance/tier-report.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
