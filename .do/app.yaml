alerts:
- rule: DEPLOYMENT_FAILED
- rule: DOMAIN_FAILED

features:
- buildpack-stack=ubuntu-22

ingress:
  rules:
  - component:
      name: farkle
    match:
      authority:
        exact: ""
      path:
        prefix: /

name: farkle-game
region: fra

static_sites:
- name: farkle
  environment_slug: node-js
  github:
    repo: artem-xox/farkle
    branch: main
    # CI is the only deploy trigger: .github/workflows/ci.yml pushes this spec
    # after typecheck, tests and the build pass. With DigitalOcean's own webhook
    # enabled as well, every push to main deployed twice — the first of them
    # ungated by tests.
    deploy_on_push: false
  source_dir: /
  build_command: npm ci && npm run build -w @farkle/web
  output_dir: apps/web/dist
  catchall_document: index.html
